---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.1
  name: cloudflarednssyncs.cfgate.io
spec:
  group: cfgate.io
  names:
    kind: CloudflareDNSSync
    listKind: CloudflareDNSSyncList
    plural: cloudflarednssyncs
    shortNames:
    - cfdns
    - dnsync
    singular: cloudflarednssync
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: Ready
      type: string
    - jsonPath: .status.syncedRecords
      name: Synced
      type: integer
    - jsonPath: .status.pendingRecords
      name: Pending
      type: integer
    - jsonPath: .status.failedRecords
      name: Failed
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CloudflareDNSSync is the Schema for the cloudflarednssyncs API.
          It manages DNS records for CloudflareTunnel resources.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: CloudflareDNSSyncSpec defines the desired state of CloudflareDNSSync.
            properties:
              cleanupPolicy:
                description: CleanupPolicy defines cleanup behavior for records.
                properties:
                  deleteOnResourceRemoval:
                    default: true
                    description: DeleteOnResourceRemoval deletes records when the
                      DNSSync resource is deleted.
                    type: boolean
                  deleteOnRouteRemoval:
                    default: true
                    description: DeleteOnRouteRemoval deletes records when the route
                      is deleted.
                    type: boolean
                  onlyManaged:
                    default: true
                    description: OnlyManaged only deletes records that were created
                      by cfgate.
                    type: boolean
                type: object
              defaults:
                description: Defaults defines default settings for DNS records.
                properties:
                  proxied:
                    default: true
                    description: Proxied enables Cloudflare proxy by default.
                    type: boolean
                  ttl:
                    default: auto
                    description: TTL is the default DNS record TTL.
                    type: string
                type: object
              fallbackCredentialsRef:
                description: |-
                  FallbackCredentialsRef references a secret containing fallback Cloudflare API credentials.
                  Used during deletion when the referenced tunnel's credentials are unavailable.
                  This enables cleanup of DNS records even if the tunnel or its secret is deleted.
                  The secret must contain CLOUDFLARE_API_TOKEN key.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret. Defaults to the resource's
                      namespace if empty.
                    type: string
                required:
                - name
                type: object
              ownership:
                description: Ownership defines how to track record ownership.
                properties:
                  comment:
                    description: Comment configures comment-based ownership.
                    properties:
                      enabled:
                        default: false
                        description: Enabled enables comment-based ownership tracking.
                        type: boolean
                      template:
                        default: managed by cfgate, tunnel={{ .TunnelName }}
                        description: Template is the comment template. Supports template
                          variables.
                        type: string
                    type: object
                  txtRecord:
                    description: TXTRecord configures TXT record-based ownership.
                    properties:
                      enabled:
                        default: true
                        description: Enabled enables TXT record ownership tracking.
                        type: boolean
                      prefix:
                        default: _cfgate
                        description: Prefix is the prefix for TXT record names.
                        type: string
                    type: object
                type: object
              source:
                description: Source defines where to get hostnames to sync.
                properties:
                  explicit:
                    description: Explicit defines explicit hostnames to sync.
                    items:
                      description: ExplicitHostname defines an explicit hostname to
                        sync.
                      properties:
                        hostname:
                          description: Hostname is the DNS hostname to create.
                          type: string
                        proxied:
                          default: true
                          description: Proxied enables Cloudflare proxy for this record.
                          type: boolean
                        target:
                          description: Target is the CNAME target. Supports template
                            variable {{ .TunnelDomain }}.
                          type: string
                        ttl:
                          default: auto
                          description: TTL is the DNS record TTL. Use "auto" for Cloudflare
                            automatic TTL.
                          type: string
                      required:
                      - hostname
                      - target
                      type: object
                    type: array
                  gatewayRoutes:
                    description: GatewayRoutes configures watching Gateway API routes.
                    properties:
                      annotationFilter:
                        description: AnnotationFilter only syncs routes with this
                          annotation.
                        type: string
                      enabled:
                        default: true
                        description: Enabled enables watching Gateway API routes.
                        type: boolean
                    type: object
                type: object
              tunnelRef:
                description: TunnelRef references the CloudflareTunnel to sync DNS
                  for.
                properties:
                  name:
                    description: Name is the name of the CloudflareTunnel.
                    type: string
                  namespace:
                    description: |-
                      Namespace is the namespace of the CloudflareTunnel.
                      Defaults to the CloudflareDNSSync's namespace.
                    type: string
                required:
                - name
                type: object
              zones:
                description: Zones defines the DNS zones to manage.
                items:
                  description: ZoneConfig defines a DNS zone to manage.
                  properties:
                    id:
                      description: ID is the optional explicit zone ID (skips lookup).
                      type: string
                    name:
                      description: Name is the zone name (e.g., example.com).
                      type: string
                  required:
                  - name
                  type: object
                type: array
            required:
            - tunnelRef
            type: object
          status:
            description: CloudflareDNSSyncStatus defines the observed state of CloudflareDNSSync.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the sync's state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failedRecords:
                description: FailedRecords is the number of records that failed to
                  sync.
                format: int32
                type: integer
              lastSyncTime:
                description: LastSyncTime is the last time records were synced.
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration is the generation observed by the
                  controller.
                format: int64
                type: integer
              pendingRecords:
                description: PendingRecords is the number of records pending sync.
                format: int32
                type: integer
              records:
                description: Records contains the status of individual DNS records.
                items:
                  description: DNSRecordStatus represents the status of a single DNS
                    record.
                  properties:
                    error:
                      description: Error contains the error message if status is Failed.
                      type: string
                    hostname:
                      description: Hostname is the DNS hostname.
                      type: string
                    proxied:
                      description: Proxied indicates if Cloudflare proxy is enabled.
                      type: boolean
                    recordId:
                      description: RecordID is the Cloudflare record ID.
                      type: string
                    status:
                      description: 'Status is the sync status: Synced, Pending, Failed.'
                      type: string
                    target:
                      description: Target is the record target/content.
                      type: string
                    type:
                      description: Type is the DNS record type (e.g., CNAME).
                      type: string
                  required:
                  - hostname
                  - proxied
                  - status
                  - target
                  - type
                  type: object
                type: array
              syncedRecords:
                description: SyncedRecords is the number of successfully synced records.
                format: int32
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
