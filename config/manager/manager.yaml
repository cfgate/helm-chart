apiVersion: v1
kind: Namespace
metadata:
  name: cloudflare-system
  labels:
    control-plane: cfgate-controller-manager
    app.kubernetes.io/name: cfgate
    app.kubernetes.io/component: manager
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cfgate-controller-manager
  namespace: cloudflare-system
  labels:
    control-plane: cfgate-controller-manager
    app.kubernetes.io/name: cfgate
    app.kubernetes.io/component: manager
spec:
  selector:
    matchLabels:
      control-plane: cfgate-controller-manager
  replicas: 1
  template:
    metadata:
      labels:
        control-plane: cfgate-controller-manager
      annotations:
        kubectl.kubernetes.io/default-container: manager
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          image: controller:latest
          args:
            - --leader-elect
            - --health-probe-bind-address=:8081
            - --metrics-bind-address=:8080
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            readOnlyRootFilesystem: true
          ports:
            - containerPort: 8080
              name: metrics
              protocol: TCP
            - containerPort: 8081
              name: health
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
      serviceAccountName: cfgate-controller-manager
      terminationGracePeriodSeconds: 10
